---
export interface Props {
  title: string;
  id: string;
}

const { title, id } = Astro.props;
---

<section id={id}>
  <h3 class="text-center font-bold uppercase pt-4 pb-2">{title}</h3>
  <hr class="mx-auto border-slate-500 border-1 w-[10%] mb-4" />
  <slot />
</section>
